name: ece549-mp

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.8
  - pip
  - ipykernel

  - numpy
  - scipy

  - matplotlib

  - scikit-image

  - pip:
    - opencv-contrib-python-headless
